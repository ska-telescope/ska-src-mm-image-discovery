apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "image-discovery.fullname" . }}-test-connection"
  labels:
    {{- include "image-discovery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "image-discovery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
